# .readthedocs.yml
version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.10"  # Specify the Python version

python:
  install:
    - requirements: requirements.txt  # Install dependencies from requirements.txt

sphinx:
  configuration: src/docs/conf.py  # Path to your Sphinx configuration file
